# Data

Put source PDFs under `data/raw/`.

For CUAD, you can copy the extracted `full_contract_pdf` directory into `data/raw/` or copy the PDFs recursively.

Evaluation questions go in `data/evaluation/questions.jsonl`. Use `questions.example.jsonl` as the template.

Do not commit confidential documents or generated indexes.
